# Example Project Specification

## Objective

Provide a deterministic command that validates a project manifest.

## Requirement

`FR-EXAMPLE-001`: The command must reject a manifest with a missing project
identifier.

## Acceptance criterion

`AC-FR-EXAMPLE-001-01`: A missing project identifier produces a non-zero exit
code and an English error message.
