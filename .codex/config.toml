# This project file is intentionally minimal and portable.
# Approval policy, sandbox mode, network permissions, personal paths, and
# machine-specific Windows settings belong to the Owner-managed user profile.
