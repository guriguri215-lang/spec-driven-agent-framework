# Evaluation Fixtures

M0 includes a minimal invalid-manifest fixture for deterministic validation
tests. M1 will add expected normalized requirements, omission metrics,
traceability checks, and comparison against an unstructured workflow.
