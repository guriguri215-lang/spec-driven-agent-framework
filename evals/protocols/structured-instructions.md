# Structured Workflow Instructions

Use SDAQF to normalize the supplied specification, trace every requirement,
apply non-compensating Must and security checks, record evidence and approvals,
and produce a bounded handoff before claiming completion.
